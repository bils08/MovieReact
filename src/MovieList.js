import React from "react"

const MovieList = (props) => {
  return
  (
    <div>
      {props.movies.map((movie, index) => {
        <div>
          <h1>{movie.</h1>
        </div>
      })}
    </div>
  )
}